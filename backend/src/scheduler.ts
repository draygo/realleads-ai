// backend/src/scheduler.ts
// Stub implementation so the backend can start without real cron jobs yet.

export function startSchedulers() {
  console.log('⏰ Schedulers started (stub implementation – no cron jobs yet)');
}
